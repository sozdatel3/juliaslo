# Julia Webinar Resources

Static GitHub Pages page for webinar recording and family protection guide resources.

## Local Preview

Open `index.html` in a browser, or serve the folder with any static server.

## Custom Domain

Add the custom domain in GitHub repository settings under `Settings -> Pages -> Custom domain`.
If GitHub creates a `CNAME` file in the repository, keep it committed.
